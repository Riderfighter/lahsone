export default class TimePeriod
{
    public start: string;
    public end: string;

    constructor(start: string, end: string)
    {
        this.start = start;
        this.end = end;
    }

    // TODO work with the Date time to convert start & end to dates
}